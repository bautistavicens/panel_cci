#ifndef _FUNCIONES
#define _FUNCIONES
#include <define.h>

extern void leer_archivo(int in_panel_id, char* mensaje);
extern void escribir_archivo(int in_panel_id, char in_mensaje[LONGITUD_MENSAJE_PANEL]);

#endif
