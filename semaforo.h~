#ifndef _SEMAFORO
#define _SEMAFORO

extern int crear_semaforo();
extern void iniciar_semaforo(int id_semaforo, int valor);
extern void esperar_semaforo(int id_semaforo);
extern void levantar_semaforo(int id_semaforo);

#endif
